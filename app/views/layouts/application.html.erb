<!DOCTYPE html>
<html>
  <head>
    <title>HAS</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!--
    <header>
   <hgroup>
     <div class="hs-hero--header">
       <div class = "has-logo">
         <a href="<%= url_for(root_path) %>">
           <h1>HAS</h1>
         </a>
       </div>
       <div class = "hs-header-home">
         <a href="<%= url_for(root_path) %>">
           <h1>HOME</h1>
         </a>
       </div>
       <div class = "hs-header-release">
         <h1>RELEASE</h1>
       </div>
       <div class="button-area">

         <% if user_signed_in? %>
           <div class="ui buttons">
             <a href="<%= url_for(users_index_path) %>">
               <button class="ui button">
                 <%= current_user.username %>
               </button>
             </a>
             <div class="or"></div>
             <%= link_to destroy_user_session_path, method: :delete do %>
             <button class="ui button" >
               ログアウト
             </button>
             <% end %>
           </div>
         <% else %>
           <div class="ui buttons">
             <a href="<%= url_for(user_github_omniauth_authorize_path) %>">
               <button class="ui button">
                 Github ログイン
               </button>
             <a>
           </div>
         <% end %>
       </div>
     </div>
   </hgroup>
 -->
 </header>
    <div class="hs-m--app">
      <div class="hs-m--app-main">
        <%= yield %>
      </div>
    </div>
　　<!-- <script type="text/javascript" src="/lib/jquery-2.1.0/js"></script> -->
    <script src="https://code.jquery.com/jquery-2.1.4.min.js"></script>

    <script type="text/javascript">
    $(function () {

      $(window).on("scroll", function () {
        if ($(this).scrollTop() > 0) {
          $(".cb-header").slideDown();
        } else {
          $(".cb-header").slideUp();
        }
      });

    });
    </script>
  </body>
</html>
